/**
 * Helper: Transforms dimensions to px
 */
export function transformDimension(value) {
    if (value === undefined) {
        return value;
    }
    if (`${value}`.endsWith('px')) {
        return `${value}`;
    }
    return `${value}px`;
}
